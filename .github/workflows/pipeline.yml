name: Deployment Pipeline

on:
  push:
    branches:
      # - master
      - todo
  
jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0